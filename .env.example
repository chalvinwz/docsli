# Copy to .env — docker compose loads it automatically.
# Entries are token:name:email, separated by ";".
# Generate tokens with: openssl rand -hex 24
DOCSLI_TOKENS=dsl_chalvin_replace_me_0000:Chalvin (agent):chalvin-agent@example.com;dsl_john_replace_me_000000:John (agent):john-agent@example.com
